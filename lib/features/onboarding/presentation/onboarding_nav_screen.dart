import 'package:flutter/material.dart';

class OnboardingNavScreen extends StatelessWidget {
  const OnboardingNavScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Онбординг')),
    );
  }
}
